- flash.each do |type, message|
  .alert.fade.in.alert-dismissable{ :class => "#{bootstrap_class_for(type)}" }
    %button.close{:type => "button", "data-dismiss" => "alert", "aria-label" => "Close" }
      %span{"aria-hidden" => true} &times;
    = message
